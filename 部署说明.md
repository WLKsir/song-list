# 歌单网页 - 部署与更新说明

本文件说明如何把歌单网页上传到 GitHub、以及如何更新 Cloudflare Pages 线上网站。

---

## 一、项目与文件说明

- **项目路径**：`D:\1.x\歌单\歌单网页`
- **网页线上地址**：https://mml-songlist.pages.dev/
- **核心文件**：
  - `index.html` —— 歌单主页面（列表/筛选/搜索/转盘入口）
  - `wheel.html` —— 歌曲转盘页面
  - `最新歌单.xlsx` —— 歌单数据源
  - `xlsx.full.min.js` —— 解析 xlsx 的库（本地引用，不依赖外网）
  - `bg.jpg` —— 背景图片
  - `启动网页.bat` / `启动穿透.bat` / `start-web.ps1` —— 本地旧版运行脚本（上线后可不用）
  - `部署说明.md` —— 本文件

> 网页是纯静态 HTML，无需构建。线上直接托管在 Cloudflare Pages。

---

## 二、所需账户与信息

| 用途 | 账号/信息 | 说明 |
|------|-----------|------|
| GitHub 用户名 | `WLKsir` | 代码托管仓库归属 |
| GitHub 仓库地址 | `https://github.com/WLKsir/song-list.git` | 仓库名 `song-list` |
| GitHub git 提交邮箱 | `17875178068@163.com` | 仅作为提交作者标识 |
| Cloudflare 账号邮箱 | `17875178068@163.com` | Cloudflare 登录账号 |
| Cloudflare Pages 项目名 | `mml-songlist` | 决定线上域名 |
| Cloudflare Pages 线上域名 | `https://mml-songlist.pages.dev/` | 对外访问地址 |
| wrangler 登录状态目录 | `D:\AI\Temp\wrangler` | 登录 token 存放处 |

> ⚠️ 安全提醒：本说明只记录账号名、邮箱等非敏感信息。**不要把密码、Token、API Key 写进本文件**，也不要把它们提交到 GitHub（仓库是公开的）。

---

## 三、上传/更新 GitHub 的步骤

前置条件：已安装 Git，且已配置全局用户名邮箱：

```bash
git config --global user.name "WLKsir"
git config --global user.email "17875178068@163.com"
```

> 首次推送时，Git 会弹出浏览器窗口，用 GitHub 账号登录授权后即可。

### 3.1 首次上传（初始化）

```bash
cd /d D:\1.x\歌单\歌单网页
git init -b main
git add -A
git commit -m "添加歌单网页"
git remote add origin https://gh-proxy.com/https://github.com/WLKsir/song-list.git
git push -u origin main
```

> 仓库地址用 `gh-proxy.com` 代理前缀是为了直连失败时也能推送（当前已配好）。

### 3.2 日常更新（改完文件后）

```bash
cd /d D:\1.x\歌单\歌单网页
git add -A
git commit -m "更新说明：本次改了什么"
git push origin main
```

### 3.3 推送失败的解决办法

若 `git push` 报 `Invalid username or token` 或 `Failed to connect`，一般是两件事之一：

**a) 凭据失效** —— 弹浏览器重新登录 GitHub 授权即可。

**b) 直连不稳定（常见）** —— 用代理推送，把 GitHub 令牌临时内嵌进 URL：

```powershell
# 从本机凭据管理器取出令牌（PowerShell）
$tok = @"
protocol=https
host=github.com

"@ | git credential fill 2>&1 | Select-String "password=" | ForEach-Object { ($_ -split "=",2)[1] }

# 通过代理推送
git push "https://WLKsir:$tok@gh-proxy.com/https://github.com/WLKsir/song-list.git" main
```

> 令牌只用于当前这条命令，不会写入文件。用完可忽略（令牌保存在系统凭据管理器里）。

---

## 四、更新 Cloudflare Pages 的步骤

线上托管在 Cloudflare Pages（项目 `mml-songlist`），用 Wrangler 命令行上传本地文件即可更新。无需构建，上传即生效。

### 4.1 前置：安装/准备

需要 Node.js（已装 v24）。wrangler 通过 npx 临时调用，无需全局安装。

### 4.2 登录 Cloudflare（首次或 token 失效时）

```powershell
$env:WRANGLER_HOME="D:\AI\Temp\wrangler"
$env:HOME="D:\AI\Temp\wrhome"
npx -y wrangler@latest login
```

执行后会弹出浏览器，用 `17875178068@163.com` 登录并点"允许"授权，出现 `Successfully logged in.` 即成功。

> 授权后那个 `welcome.developers.workers.dev` 页面打不开属正常现象（该域名被墙），不影响登录。

### 4.3 上传部署（每次更新执行）

```powershell
$env:WRANGLER_HOME="D:\AI\Temp\wrangler"
$env:HOME="D:\AI\Temp\wrhome"
npx -y wrangler@latest pages deploy "D:\1.x\歌单\歌单网页" --project-name=mml-songlist --branch=main
```

看到 `✨ Deployment complete!` 即成功，约 1 分钟后线上生效。

> 注意：`--branch=main` 部署到生产环境。若不加会作为预览部署。

### 4.4 验证

浏览器打开 https://mml-songlist.pages.dev/ 检查页面和歌单数据是否正常。